# HeadlessX path — adjust if your clone is elsewhere
HEADLESSX_REPO_PATH=/path/to/headlessx/repo

# HeadlessX API credentials — replace with your own keys
DASHBOARD_INTERNAL_API_KEY=replace-with-your-api-key
CREDENTIAL_ENCRYPTION_KEY=replace-with-your-encryption-key
